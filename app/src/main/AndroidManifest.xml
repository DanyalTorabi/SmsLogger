<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smslogger">

    <!-- Permissions to read SMS -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true"/>
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- Permission to receive SMS broadcasts (for real-time updates) -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!-- Permission for Foreground Service (if you need continuous monitoring) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Required for Android 13+ for foreground service notifications -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsLogger"
        tools:targetApi="28" >
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--    -->
<!--        &lt;!&ndash; Declare your Service &ndash;&gt;-->
        <service
            android:name=".service.SmsLoggingService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:ignore="ForegroundServicePermission" />
<!--        &lt;!&ndash; Set exported="true" if you need to start it from other apps,-->
<!--             but for local logging, false is safer. &ndash;&gt;-->
<!--    -->
<!--        &lt;!&ndash; Declare your BroadcastReceiver for incoming SMS &ndash;&gt;-->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS"> <!-- More specific permission for security -->
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>